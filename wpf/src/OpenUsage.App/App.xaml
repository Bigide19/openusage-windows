<Application x:Class="OpenUsage.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
             xmlns:converters="clr-namespace:OpenUsage.App.Converters"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="/Assets/tray-icon.ico"
                            ToolTipText="OpenUsage">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Show Stats" Command="{Binding ShowStatsCommand}" />
                        <MenuItem Header="Settings" Command="{Binding ShowSettingsCommand}" />
                        <Separator />
                        <MenuItem Header="About OpenUsage" Command="{Binding ShowAboutCommand}" />
                        <Separator />
                        <MenuItem Header="Quit" Command="{Binding QuitCommand}" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>

            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
            <converters:NullToCollapsedConverter x:Key="NullToCollapsedConverter" />
            <converters:ProgressWidthConverter x:Key="ProgressWidthConverter" />
            <converters:ProgressFormatConverter x:Key="ProgressFormatConverter" />
            <converters:PaceDotColorConverter x:Key="PaceDotColorConverter" />
            <converters:ColorStringToBrushConverter x:Key="ColorStringToBrushConverter" />
            <converters:ResetTimeConverter x:Key="ResetTimeConverter" />
            <converters:EnumBoolConverter x:Key="EnumBoolConverter" />
            <converters:FirstCharConverter x:Key="FirstCharConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
